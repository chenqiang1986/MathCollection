apiVersion: v1
kind: Service
metadata:
  name: math-mistake-tracker
  namespace: __NAMESPACE__
spec:
  type: LoadBalancer
  selector:
    app: math-mistake-tracker
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
